apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: ruby-app
spec:
  selector:
    app: frontend
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
  type: NodePort

